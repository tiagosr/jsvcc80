### 🧪 Test Results
- 128 passing tests (1 pre-existing failure: float array initializer parsing)

### ✅ Completed
- **`bool` type, `true`/`false`/`nullptr` keywords, and `nullptr_t` builtin typedef** - `true`, `false`, `nullptr`, `bool` added to Keywords Set in tokenTypes.js; `bool` added to TypeInfos with size 1 and to basicType parser in type-system.js; `'bool': 1` added to TypeSpecNode.TypeSizes in nodes.js; `nullptr`, `true`, `false` keyword handling as integer literals (1/0) in expr-primary.js; `bool` added to extended basicType in function-pointers.js; `nullptr_t -> bool` builtin typedef registered in type-registry.js; `nullptr_t` added to builtin typedef names in cparser.js; 24 new tests covering bool declarations, true/false expressions, nullptr expressions, nullptr_t typedef, sizeof(bool), casting, arrays, control flow, and end-to-end compilation
- **`#embed` `if_empty` attribute** - Optional attribute `if_empty(val1, val2, val3)` that emits substitute values when the embedded file is empty (0 bytes); `_processEmbedAttribute` parses comma-separated values with special `comma` keyword support; `_handleEmbed` returns items array instead of bytes when file is empty and if_empty is set; `_tokenizeEmbedResult` handles mixed byte/comma items; 10 new tests covering empty file substitution, non-empty file override, single/multiple values, error cases, combined with other attributes, conditional block interaction
- **`#embed` C23 binary file inclusion preprocessor directive** - `_handleEmbed` method in `directives.js` parses file reference (quote/angle), optional attributes (`limit(n)`, `offset(n)`, `prefix(text)`, `suffix(text)`) with nested parenthesis support; binary file reading with offset/limit byte extraction; `_tokenizeEmbedResult` in `lexer.js` converts bytes to INTEGER tokens with prefix/suffix tokenization; `_tokenizeString` helper tokenizes arbitrary text into lexer tokens; macro expansion in embed line content; 8 new test suites covering basic embed, limit, offset, combination, prefix, suffix, edge cases, errors, conditional blocks, location tracking, multiple embed, and macro interaction

- **Array initializer lists** - `InitializerNode` AST type for brace-enclosed initializer lists; parser `braceInit` rule rewritten to accept expressions and nested initializers via `initializerElement` with `lazy()` circular dependency resolution; global `variableDecl` extended to accept brace initializers; `translateExpressionValue` extended to recursively extract values from `InitializerNode`; `translateInitializer` method added for local array element storage sequences; `translateDecl` extended with `elemSize` and `baseType` fields; `generateGlobal` extended to emit `.db/.dw/.dd` directives for array initializers based on element size; 22 new tests

- **Soft-float trigonometric and exponential operations** - Z80 assembly implementations for trigonometric functions (`sinf`, `cosf`, `tanf`, `cotf`, `asinf`, `acosf`, `atanf`, `atan2f`) and exponential/power functions (`sqrtf`, `expf`, `powf`, `logf`, `log10f`); `float.h` extended with all new C API declarations and internal `_float_*` function declarations; Expression translator updated with `floatFuncMapping` for all 13 new functions and `translateFloatCall` extended to handle multi-argument functions (`atan2f`, `powf`); Fixed `translateFloatCall` to handle parser's single-node args storage (converted to array); 36 new tests for trigonometric and exponential functions

- **Deep call nesting compilation verified working** - Tested 20-level deep call nesting (`f20→f19→...→f1`) compiles without failure; CLI `-v/--version` option conflict fixed (line 111 in `bin/vcc80.js` had duplicate `-v` short flag conflicting with `-v/--verbose` on line 105); 9 new tests for version flag
- **Token line:column location tracking** - `_tokenizeReplacement()` in `src/preprocessor/lexer-core.js` now tracks line:column positions for macro expansion tokens; position variables (`line`, `column`) increment per character consumed, newline resets column and increments line; location objects created with `{ file, start: { line, column }, end: { line, column } }` for all token types (identifiers, integers, strings, operators, ##, #); function-like macro tokens from `_tokenizeReplacement` have proper locations; `##` operator preserves location from left operand; `#` stringification preserves location from argument token; 13 new tests
- **Object file viewer** - `src/disassembler/objectfile_viewer.js` with ObjectFileViewer class (constructor with baseAddress/verbose/sections options, view/viewSection/viewAll methods, createSectionView helper), ObjectFileView class (fileName/format/architecture/sectionHeaders/sections Map/symbols/relocations, toJSON/toString producing objdump-like output with file header, section headers table, disassembly with hex bytes, data hex dumps with ASCII, symbol table, relocation table), SectionView class (name/type/baseAddress/dataBytes/instructions/hexDumpLines, toJSON/toString), convenience function `viewObjectFile()`, hex formatting helpers (formatHex with $ prefix for addresses, formatHex2 plain hex for individual bytes), section header formatting with column alignment, code section disassembly with hex bytes alongside mnemonics, data section hex dump with ASCII representation, BSS section .ds directive output, symbol table with value/size/type/visibility/name columns, relocation table with section/offset/symbol/type columns, section filter support for viewing specific sections only, 33 new tests
- **Typedef struct/union parsing fix** - Parser's `collectTypedefNames` method extended to handle `typedef struct/union {...} name;` syntax by skipping brace-enclosed field blocks; `TypeRegistry` extended with `registerStructFromTypedef()` method to register anonymous struct typedefs using the declaration name; `DeclNode` extended with `structNode` getter that returns the `init` field when it's a `StructNode`; `StatementTranslator.translateDeclStmt()` updated to use `resolved.structType` and `resolved.structKind` from resolved typedef instead of original `typeInfo` for proper struct type propagation in symbol table; 5 new tests
- **FILE callback-based OS abstraction** - FILE struct redesigned from 12 bytes with raw data fields to 18 bytes with callback function pointers: `streamType` (char), `flags` (char), `port` (int for serial/terminal context), `read` (int callback), `write` (int callback), `close` (int callback), `eof` (int callback), `error` (int callback), `available` (int callback), `flush` (int callback); All FILE intrinsics (FPUTC, FGETC, FEOF, FERROR, FOPEN, FCLOSE, SERIAL_OPEN, SERIAL_CLOSE, SERIAL_READ, SERIAL_WRITE, SERIAL_AVAILABLE, TERMINAL_OPEN, TERMINAL_CLOSE) rewritten to use indirect call convention: load callback pointer from FILE struct offset, push args on stack, `push hl` + `ret` for indirect call, stack cleanup; Platform implementations (filesystem, serial, terminal) provide callback function names (`fs_read`, `fs_write`, etc.) resolved by linker; `file.h` header updated with new struct definition; `type-registry.js` updated with new FILE struct registration (18 bytes, 10 fields); `z80codegen.js` intrinsic handlers fully rewritten; 19 tests updated
- **Soft-float nearest integer, absolute value, and decomposition functions** - Z80 assembly implementations for `fabsf` (`float_abs.z80`), `floorf` (`float_floor.z80`), `ceilf` (`float_ceil.z80`), `modff` (`float_modf.z80`), `frexpf` (`float_frexpf.z80`), `ldexpf` (`float_ldexpf.z80`); `float.h` extended with C API declarations (`fabsf`, `floorf`, `ceilf`, `modff`, `frexpf`, `ldexpf`) and internal `_float_*` function declarations; Expression translator updated with `translateFloatCall` method and `translateCallNode` detection for float-returning functions, emitting float data labels and `CallInstruction` with `{ floatResult: true }` meta; IEEE 754 algorithms handle special cases (NaN, Infinity, zero), sign handling, mantissa overflow, and exponent adjustments; 42 new tests
- **Function inlining semantics and `inline` keyword** - Parser handles `inline` keyword before function return type (storage class specifier); `FunctionNode` extended with `isInline` property; InlineOptimizer optimization pass created that inlines functions marked with `inline` keyword and functions called only once (configurable via `inlineOnce` option); Pass only activates with `inline` flag (O2/O3 optimization levels); `isInline` metadata propagated through AST-to-IR translation; IR `CallInstruction` replaced with inlined function body instructions with parameter-to-register mapping; Register pass registration updated; 15 new tests
- **IEEE 754 single-precision `float` type support** - Float literal parsing fixed in lexer.js (detects `digit...digit.digit...digit` and `.digit...digit` patterns); Float type declarations parsed; Float arithmetic operations (`+`, `-`, `*`, `/`, `%`) translated to external `_float_add`, `_float_sub`, `_float_mul`, `_float_div`, `_float_mod` calls; Float comparison operations (`==`, `!=`, `<`, `>`, `<=`, `>=`) translated to `_float_eq`, `_float_ne`, `_float_lt`, `_float_gt`, `_float_le`, `_float_ge` calls; Float literal storage as 4-byte IEEE 754 data labels (`flt_`); Float variable loads/stores emit 4-byte sequences; Float special value checks (`_float_isnan`, `_float_isinf`); Z80 soft-float assembly library created in `src/core/stdlib/` (`float_add.z80`, `float_sub.z80`, `float_mul.z80`, `float_div.z80`, `float_mod.z80`, `float_isnan.z80`, `float_isinf.z80`, `float_eq.z80`, `float_ne.z80`, `float_lt.z80`, `float_gt.z80`, `float_le.z80`, `float_ge.z80`); `float.h` standard header created with constants and function declarations; Expression translator tracks `isFloat` flag for float operand detection; 27 new tests
- **Calling convention fixes** - Fixed `call-memory.js` new_sdcc register assignment for 2+ args: first byte arg => A, second byte arg => L, first word arg => HL, second word arg => DE (previously all args went to same register pair); Fixed `z80codegen.js` `generateUnaryOp` and `generateBinaryOp` mov handlers to emit `ld d/e/b/c, src` for physical 8-bit registers instead of storing to memory `(d)`/`(e)`; Fixed `generateCalleeCall` to remove caller stack cleanup (callee convention: cleanup happens in callee epilogue, not caller); Fixed `calling_convention_codegen.test.js` cdecl test assertions (tests 1-2 had inverted assertions expecting new_sdcc behavior for __cdecl__ functions); 5 tests fixed, 1437 total passing
- **Compound assignment operators (`+=`, `-=`, `*=`, `/=`, `%=`, `&=`, `|=`, `^=`, `<<=`, `>>=`)** - Parser extended to accept all 10 compound assignment operators in `expr-assign.js`; IR translation added `translateCompoundAssignment()` method that expands `x op= y` to load-op-store pattern (load x, compute x op y, store result to x); Z80 codegen produces correct load/operation/store sequences for all operators; 24 new tests
- **Stack argument and return passing codegen fixes** - Fixed SP restoration from `ld sp, l` + `ld sp, h` (corrupts stack) to `ld sp, hl` (correct Z80 instruction); added `ret_val` special case in `generateLoad` to handle return value loading from A register; fixed `generateCdeclCall`, `generateCalleeCall`, `generateNewSdccCall`, `generateFastcallCall` to emit correct return value handling; added `mov` and `count` opcode handlers in `generateBinaryOp` and `generateUnaryOp`; fixed `call-memory.js` to use `argResult.result` as `mov` source and push physical register names (`h`, `l`, `a`) as call args; simplified call generation methods to avoid redundant arg loading (IR already handles via PushInstruction/mov); fixed intrinsic handlers (FOPEN, SERIAL_OPEN, TERMINAL_OPEN) SP restoration; fixed `extractCallingConventionFromAttributes` to recognize `__cdecl__`, `__fastcall__`, `__callee__`, `__new_sdcc__` attribute names; 18 new tests
- **Scope-aware identifier resolution** - `SymbolTable.define()` allows symbol shadowing across nested scopes; `TranslationState` extended with `pushScope()`/`popScope()` methods; `StatementTranslator.translateCompound()` pushes/pops scope for compound statements; `ControlFlowTranslator` pushes/pops scopes for if/else bodies, while bodies, for loop init and body, do-while bodies, and switch case/default clauses; nested compound statements and deeply nested control flow all create proper symbol scope chains; 13 new scope-shadowing tests + 4 updated qualifier tests
- **Z80 simulator breakpoint functionality** - `CallTracker` module for call depth tracking, `BreakpointManager` module for breakpoint storage; `stepInto()` executes CALL/RST and continues into called function, `stepOver()` executes CALL and all nested calls until matching RET, `runToBreakpoint()` runs until breakpoint hit; CALL/RET/RST/RETI/RETN handlers integrated with call tracker; breakpoints checked before PC advancement; 49 new tests
- **`unsigned:n` bit field type support** - Parser handles `unsigned:8`, `unsigned:16`, `unsigned:32` syntax with qualifiers (`const`, `volatile`); `TypeSpecNode.bitWidth` field now used for size calculation (`ceil(bitWidth/8)`); `typeString()` displays `unsigned:n` format; IR translation propagates correct sizes for bit-width types; Z80 codegen uses 8-bit vs 16-bit operations based on computed size; 42 new tests
- **Symbol map compiling and exporting for debugging** - `--map` CLI flag to export link map to file; `ObjectSymbol`, `ResolvedSymbol`, and `MapSymbol` extended with `size`, `line`, and `sourceFile` fields; line numbers propagated from AST through IR translation to object file and link map; binary object file format extended to serialize/deserialize size, line, and sourceFile for symbols; JSON link map includes debugging info (symbol size, source line, source file); 15 new tests
- **FILE OS abstraction - stream types (serial port, terminal, filesystem)** - Extended `FILE` struct from 8 to 12 bytes with `streamType` (0=filesystem, 1=serial, 2=terminal), `port` (serial I/O address), and `device` (terminal identifier) fields; Added intrinsics `__serial_open`, `__serial_close`, `__serial_read`, `__serial_write`, `__serial_available`, `__terminal_open`, `__terminal_close`; Z80 codegen for serial uses IN/OUT port instructions; Updated existing FILE intrinsics (FPUTC, FGETC, FEOF, FERROR, FOPEN, FCLOSE) to new 12-byte layout; 41 new tests
- **Struct member access (`.`) and pointer member access (`->`) IR→Assembly** - Fixed `translateMember` and `translatePointerMember` in call-memory.js to properly handle struct field offset lookup for both IdentifierNode and computed pointer objects; Fixed `translateAssignment` to use `DEREF_STORE` for struct member assignments; Added Z80 codegen support for `member`/`pmember` fallback opcodes; Fixed `generateLoadAddr` to store 16-bit addresses to memory; Fixed `generateLoad`/`generateStore`/`generateDerefLoad`/`generateDerefStore` to handle physical registers, 8-bit values, and 16-bit memory loads; Extended register allocator to handle `LoadAddrInstruction`, `DerefLoadInstruction`, `DerefStoreInstruction`, `IndexedLoadInstruction`, `IndexedStoreInstruction`; Fixed register allocator `allocateLoadAddr` to update vregToPreg mapping; Fixed peephole dead store elimination to preserve stores to variable names not overwritten within block; 8 new end-to-end tests
- **Disassembler CLI integration** - `bin/vcc80.js` extended with `--disassemble` flag and `--base` address option; supports disassembling .o object files and .bin flat binaries; imports ObjectFileDisassembler and BinaryDisassembler modules; 159 new tests total
- **Z80 disassembler comprehensive test suite** - `src/tests/z80disassembler.test.js` with 56 tests covering standard opcode disassembly (NOP, LD, INC, DEC, ALU, jumps, calls, RET, RST, EI/DI, DAA, SCF, CCF, CPL), CB-prefixed opcodes (RLC/RRC/RL/RR/SLA/SRA/SLL/SRL/BIT/RES/SET), ED-prefixed opcodes (ADC/SBC/IM/LD A,I/R/NEG/INI/OUTI/INIR/OTIR/INDR/OTDR/LDIR/LDDR/RETN), DD/IX-prefixed opcodes (LD IX,nn, ADD IX,BC, INC/DEC IX, LD (nn),IX, IX,(nn), ADD IX,SP, JP (IX), (IX+d) variants, FD/IY-prefixed opcodes, DD/FD CB-prefixed opcodes, Z80Instruction class (constructor/toJSON), Z80Disassembler class (disassemble/disassembleRange/disassembleAt/baseAddress/symbols), symbol resolution, disassembleToLines helper, edge cases (single byte, long stream, mixed instructions, valid opcodes, Uint8Array input)
- **Object file disassembler** - `src/disassembler/objectfile_disassembler.js` with ObjectFileDisassembler class (constructor with baseAddress/verbose options, disassemble/disassembleSection/disassembleAll methods), ObjectFileDisassembly class (sections Map, symbols array, relocations array, toJSON/toString with symbol resolution, _symbolMap getter), SectionDisassembly class (name/type/baseAddress/instructions/dataBytes/relocations, toJSON/toString with .db/.ds directives for data sections), convenience function `disassembleObjectFile()`, `buildSymbolMap()` helper, symbol name resolution for section base addresses, formatted output with section headers and relocation annotations in verbose mode
- **Disassembler integration tests** - `src/tests/disassembler.integration.test.js` with 103 tests across 16 describe blocks covering ObjectFileDisassembler code/data sections, ObjectFileDisassembly/SectionDisassembly classes, symbol resolution, disassembleObjectFile helper, BinaryDisassembler basic/range/single instruction, BinaryDisassembly/BinarySection classes, bytesToHex helper, object file round-trip serialization, binary with symbols, verbose mode, and edge cases (64KB binary, mixed instructions, multiple sections, empty sections)
- **Binary disassembler** - `src/disassembler/binary_disassembler.js` with BinaryDisassembler class (constructor with baseAddress/symbols/verbose/sectionBreaks options, disassemble/disassembleRange/disassembleAt methods, buildSymbolMap/resolveAddress/formatInstruction/formatOutput methods), BinaryDisassembly class (instructions/sections/symbols/baseAddress/totalSize/toJSON/toString), BinarySection class (name/startAddress/endAddress/bytes/size/toJSON), convenience functions `disassembleBinary()` and `disassembleBinaryFromFile()`, `bytesToHex()` helper, address-to-symbol resolution, optional section headers, verbose hex dump alongside assembly
- **Z80 disassembler** - `src/disassembler/z80disassembler.js` with comprehensive opcode tables (standard 0x00-0xFF, CB-prefixed, ED-prefixed, DD/IX-prefixed, FD/IY-prefixed), Z80Instruction class with opcode/mnemonic/operands/bytes/length/address/toJSON, Z80Disassembler class with disassemble/disassembleRange/disassembleAt methods, symbol resolution, operand formatting (immediate values, addresses, registers, displacements), convenience functions `disassemble()` and `disassembleToLines()`
- **`__sdcc__` calling convention as default** - `CALLING_CONVENTION_DEFAULT` changed from `cdecl` to `new_sdcc`; all functions without explicit calling convention attribute now use new_sdcc (up to 2 args in registers, spillover on stack); Z80 codegen default fallback cases updated to new_sdcc; function pointer indirect calls use cdecl (no paramType info for register assignment); 11 new tests for default behavior
- **Function calling conventions** - `__attribute__(__cdecl__("name"))`, `__attribute__(__fastcall__))`, `__attribute__(__callee__))`, `__attribute__(__new_sdcc__))` parsing, IR metadata, Z80 codegen for cdecl (stack push, caller clears), fastcall (single arg on registers), callee (stack push, callee clears), new_sdcc (register spillover up to 2 args), 35 new tests
- Preprocessor/lexer with pragma support (`#pragma once`, `#pragma pack`)
- PEG parser combinator framework (seq, alt, many, some, opt, lit, any, pred, lazy, map)
- AST node definitions for full C syntax
- Nanopass IR with instruction classes and symbol tables
- Z80 code generator backend (LOAD, STORE, binary ops, jumps, calls)
- Plugin architecture interfaces
- C grammar PEG parser with proper forward reference handling via `lazy()`
- AST construction via `map()` combinator - all parsed results converted to proper AST nodes
- AST → IR translation pass (`src/nanopass/ast_to_ir.js`) - functions, declarations, expressions, control flow
- AST → IR translation extended - binary ops with temp register tracking, function calls with argument pushing, while/do-while/for loops, switch/case, break/continue, goto/labels, assignment expressions, conditional (ternary) expressions
- C grammar parser binary expression rules fixed - proper right operand parsing with correct precedence levels
- C grammar parser operator mapping - relational (< > <= >=) to (lt gt le ge), equality (== !=) to (eq ne), bitwise (& | ^) to (and or xor), logical (&& ||) to (land lor), unary (- ~ ~) to (neg not lognot)
- C grammar parser extended with while/do-while/for loops, switch/case, goto/break/continue, struct/union/enum/typedef
- Z80 optimization passes - peephole optimizer (dead code elimination, constant folding, redundant move/jump elimination, stack op merging) and register allocator (physical Z80 register assignment with spilling)
- Optimization pass registration via global registry
- **End-to-end pipeline wired** - `compiler.js` integrates lexer → parser → AST → IR → optimization → codegen
- **Binary operator mapping** - C operators (+, -, *, /, %, <<, >>) mapped to IR opcodes (add, sub, mul, div, mod, shl, shr)
- **Comparison codegen** - Z80 comparison operations (lt, gt, le, ge, eq, ne) generate proper cp/jp sequences
- **Register allocator cross-block** - Fixed virtual register tracking across basic block boundaries
- Parser extended for typed function parameters (int a, int b) with void support
- **Object file format** - `src/linker/objectfile.js` with ObjectFile, ObjectSection, ObjectSymbol, ObjectRelocation classes and IrToObjectFile converter
- **WLA DX code generator** - `src/linker/wladxcodegen.js` generates WLA DX-compatible Z80 assembly with section directives, DB/DW/DS data, and relocation support
- **Linker** - `src/linker/linker.js` combines object files, resolves symbols/relocations, generates binary/WLA DX output and link maps
- **Z80 codegen quality improvements** - Proper stack frame management using IX as frame pointer, basic block label emission, fixed comparison logic (le/ge flags), valid Z80 stack alloc/free instructions, complete division/mod/shl/shr implementation, copy propagation optimization, fixed constant folding with correct IR op names
- **Linker wired into compiler pipeline** - CLI flags `-c` for compile-only (object file output), `--format` for output format selection (assembly/wladx/binary), `--map` for link map generation, multi-file compilation with automatic linking
- **Static library (.a) support** - `src/linker/archive.js` with Archive/ArchiveMember classes, VCC80A binary format, serialization/deserialization, CLI handling of .a files, Compiler.loadArchive method, and 22 archive tests
- **const, volatile, and register qualifiers** - `TypeSpecNode` extended with `isVolatile` field (alongside existing `isConst`); `DeclNode` and `ParameterNode` extended with `storageClass` field for `register`; parser handles `const`, `volatile`, and `const volatile` type qualifiers before type keywords; `register` storage class specifier parsed for declarations and function parameters; qualifiers propagated through IR symbol table entries; `typeString()` includes qualifier prefixes; 29 new tests
- **Struct, union, sizeof, offsetof, typeof** - `SizeOfNode`, `OffsetOfNode`, `TypeOfNode` AST nodes; `TypeSpecNode` extended with `structType`/`structKind` for struct/union type references; struct/union tag collection and type registry with field offset computation; parser handles `sizeof(type)`, `sizeof(expr)`, `offsetof(Type, field)`, `typeof(expr)`, `struct Tag { ... }`, `union Tag { ... }`, struct variable declarations, member access with offset resolution; lexer `->` operator tokenized; 48 new tests
- **Pointers, arrays and character strings** - TypeSpecNode extended with `pointerDepth`, `isArray`, `arrayLength`; parser handles pointer/array declarators (`int *p`, `int arr[10]`), address-of operator (`&`), dereference (`*`), string literals emitted as global `.db` data; new IR instructions (`LoadAddrInstruction`, `DerefLoadInstruction`, `DerefStoreInstruction`, `IndexedLoadInstruction`, `IndexedStoreInstruction`); Z80 codegen for pointer operations and indexed memory access; 34 new tests
- **typedef and default types** - Parser supports `void`, `char`, `short`, `long`, `unsigned`, `signed`, `_Bool` as type specifiers with signedness modifiers; typedef names resolved via two-pass parsing; TypeSpecNode tracks type sizes; typedef aliases resolved in IR translation; 50 new tests
- **Binary object file format** - `src/linker/objectfile_loader.js` with VCC80O magic header, serialization/deserialization of sections, symbols, relocations, and CLI support for loading .o files
- **Processor intrinsics** - `IntrinsicInstruction` IR class, intrinsic detection in `translateCall`, Z80 codegen for special opcodes and port access, 43 intrinsic tests
- **Block transfer intrinsics** - `IntrinsicMap` extended with `__ini`, `__outi`, `__inir`, `__otir`, `__ind`, `__outd`, `__indr`, `__otdr`; Z80 codegen for INI/OUTI/INIR/OTIR/IND/OUTD/INDR/OTDR block transfer instructions using B for count, C for port, HL for buffer; 19 new tests
- **Preprocessor directives** - `#define`, `#undef`, `#ifdef`, `#ifndef`, `#else`, `#endif` implemented in lexer; `PreprocessedSource` extended with `conditionalStack`, `skipDepth`; `readDirective()` replaces `readPragma()` with generalized directive dispatcher; nested conditionals with skip depth tracking; 40 new tests
- **Include directive** - `#include "file"` and `#include <file>` implemented in lexer; `PreprocessedSource` extended with `includePaths` and `includedFiles` tracking for duplicate prevention; file resolution supports local directory, system include paths, and recursive inclusion; macros propagate across include boundaries; `-I` CLI flag for include directories; 15 new tests
- **#if/#elif/defined() preprocessor** - `#if` and `#elif` directives with full constant expression evaluation; recursive descent parser supports arithmetic (+, -, *, /, %), relational (<, >, <= >=), equality (== !=), bitwise (&, |, ^, ~), logical (&&, ||), unary (+, -, !, ~), shift (<<, >>), hex/octal literals, parenthesized expressions; `defined(MACRO)` and `defined MACRO` operators; undefined macros evaluate to 0 in expressions; macro values substituted in expressions; conditional stack frames track `branchTaken` for correct `#elif`/`#else` interaction; 40 new tests
- **Function-like macros with parameter substitution** - `#define MAX(a,b) ((a)>(b)?(a):(b))` function-like macro parsing with parameter list extraction; macro expansion during tokenization with object-like and function-like macro support; parameter substitution in replacement text; `#` (stringification) operator converts argument to string literal; `##` (token pasting) operator concatenates tokens; recursive macro expansion with rescan after substitution; infinite recursion guard prevents self-referencing macros; built-in macros `__FILE__` and `__LINE__` with proper line tracking; 39 new tests
- **Variadic function signatures** - Parser handles `func(...)` with ellipsis-only parameters; Parser handles `func(int a, int b, ...)` with named params followed by ellipsis; ParameterNode tracks `isVariadic` flag for ellipsis parameter; FunctionDefinitionNode computes `isVariadic` from parameters; Z80Codegen tracks variadic functions via currentFunctions Map; Variadic functions skip stack cleanup in CALL instruction (caller responsibility); IR excludes variadic param names from parameter list
- **Function pointers - Type system foundation** - Extended `TypeSpecNode` with `isFunctionPointer`, `functionReturnType`, and `functionParams` fields for representing function pointer types; Added `createFunctionPointerType()` helper to construct function pointer type specifications; Updated `getSize()` and `getElementSize()` to return 2 bytes for function pointers (16-bit Z80 addresses); Enhanced `typeString()` to format function pointers as `returnType (*)(paramTypes)`; Function pointers have `pointerDepth = 1` and `isFunctionPointer = true`; All type system operations properly handle function pointer types with correct size calculations; Comprehensive test suite covering function pointer type creation, size queries, string representations with various qualifiers (const/volatile), parameter lists, and edge cases
- **Function pointer declarator parsing** - Enhanced parser to support full function pointer declarators including array dimensions (`int (*fp[3])(int)`); Fixed parser issues causing test failures; Added proper handling for `(*name)(params)` pattern with optional array dimension in function pointer declarations; 7 out of 8 function pointer tests now passing
- **Nested function pointer parsing** - Fixed parsing of pointer-to-function-returning-function-pointer declarations (`int (*(*fp)(int))(int)`); Implemented custom `FunctionPointerDeclaratorParser` class with recursive nested parsing, lookahead for nested vs simple function pointers, and proper token value extraction; All 8 function pointer tests passing
- **Function pointer IR translation** - Implemented complete IR translation for function pointers: address-of function (`&func`) returns function pointer, storing function pointers to variables via `LOAD_ADDR` + `BINOP addr` pattern, loading function pointers from variables, calling through function pointers (both direct and through array elements `fp[i](args)`), function pointer parameters, and function pointer local variable declarations with initialization; Fixed parser bug where function pointer declaration names were not extracted correctly; 6 new IR translation tests + 13 existing type system tests passing
- **Z80 code generation for function pointers** - Added `CallIndirectInstruction` IR class for function pointer calls, updated `translateLoadFunctionPointer` to use `DerefLoadInstruction` for proper loading, implemented `generateCallIndirect` in Z80 codegen that loads function address from register and calls through it, added register allocator support for `CallIndirectInstruction`, updated IR translation to use `CallIndirectInstruction` instead of `CallInstruction` for function pointer calls, fixed old `generateCall` to only handle regular (non-pointer) function calls; 2 new end-to-end tests for Z80 code generation
- **Processor intrinsics** - `IntrinsicInstruction` IR class, intrinsic detection in `translateCall`, Z80 codegen for special opcodes and port access, 43 intrinsic tests
- **Block transfer intrinsics** - `IntrinsicMap` extended with `__ini`, `__outi`, `__inir`, `__otir`, `__ind`, `__outd`, `__indr`, `__otdr`; Z80 codegen for INI/OUTI/INIR/OTIR/IND/OUTD/INDR/OTDR block transfer instructions using B for count, C for port, HL for buffer; 19 new tests
- **Setjmp/Longjmp/Alloca intrinsics** - `IntrinsicMap` extended with `__setjmp`, `__longjmp`, `__alloca`; Z80 codegen for context save/restore (PC, SP, IX, HL, DE, BC, AF) for setjmp/longjmp, stack allocation for alloca; 12 new tests
- **C standard library - memory operations** - `memset`, `memcpy`, `memcmp`, `memmove`, `memchr` implemented as C source files in `src/core/stdlib/`; parser support for C-style casts `(type)expr`, postfix increment/decrement (`x++`, `x--`), brace-enclosed array initializers `{1, 2, 3}`, and `NULL` keyword; 10 new tests
- **Z80 cycle-unaware simulator** - `src/simulator/` with CPU state management, 64KB flat memory with hooks, port I/O callback system, memory watch/probe manager, and instruction execution engine. Supports LD (8/16-bit), ALU (ADD/SUB/AND/OR/CP), PUSH/POP, JP/JR/CALL/RET, HALT, rotations (RLCA/RRA/RLA/RRCA), CPL, CCF, DAA, IN/OUT, RST, EI/DI, EXX, EX (SP),HL. 40 passing tests.
 - **crt0 startup code** - `src/linker/crt0.js` generates Z80 startup routine that initializes stack pointer, zeros BSS section, calls `main()`, and halts/loops after return. Linker integrates crt0 at base address by default. CLI flags `--no-crt0` and `--stack-top <addr>` for configuration. 14 new crt0 tests + 7 linker integration tests passing.
 - **Z80 simulator - flags register operations** - `getFlag()`, `setFlag()`, `clearFlag()` methods on Simulator class for test access; flags register state management.
 - **Z80 simulator - EX AF, AF' (0x08)** - Implemented swapping `f` with `shadow.f`; added `shadow.f` field to CPU state reset.
 - **Z80 simulator - IM 2 (ED 7E)** - Implemented setting interrupt mode 2.
 - **Z80 simulator - Memory watch integration** - WatchManager now triggered on all data memory reads/writes (not just instruction fetches); fixed constructor ordering in Simulator (watcher created before memory.watcher assignment).
  - **Z80 simulator - RES/SET instructions (CB 80-FF)** - Implemented RES b,r (ED 80-BF) and SET b,r (ED C0-FF) for all registers and (HL) memory references;
  - **Z80 simulator - Sign flag (S) and conditional instructions** - Added `SIGN: 7` to Flags object, added `clearFlag()` method, fixed `setFlags8()` to set S flag, fixed all ALU operations (ADD/ADC/SUB/SBC/AND/XOR/OR/CP) to set S flag, fixed INC/DEC/RLCA/RRCA/RLA/RRA to set S flag, fixed shift/rotate CB-prefixed instructions to set S flag, corrected JP P/M (0xC2/0xEA) to check parity/sign flags respectively, corrected CALL P/M (0xC4/0xEC) and RET P/M (0xC0/0xE0) to check correct flags, fixed JR P/M (0xE2/0xE8) to check parity/sign flags; 11 new tests passing
- **Z80 simulator - DD/FD (IX/IY) initial support** - Implemented `LD IX/IY, nn`, `LD (nn), IX/IY`, `LD IX/IY, (nn)`, `LD IX/IY, BC/DE`, `LD BC/DE, IX/IY`, `LD SP, IX/IY`, `PUSH IX/IY`, `POP IX/IY`, `INC IX/IY`, `DEC IX/IY`, `ADD IX/IY, HL`, `JP (IX/IY)`; 16 new tests passing
- **Z80 simulator - DD/FD (IX/IY) Logic/arithmetic/bit operations** - Implemented `INC (IX/IY+d)`, `DEC (IX/IY+d)`, `ADD/ADC/SUB/SBC A, (IX/IY+d)`, `AND/OR/XOR/CP (IX/IY+d)`, `RLC/RRC/RL/RR (IX/IY+d)`, `SLA/SRA/SLL (IX/IY+d)`, `SET/RES/BIT b,(IX/IY+d)`
- **Linker map files (JSON-based format)** - `src/linker/mapfile.js` with MapSection, MapSymbol, MapRelocation, LinkMap classes, bytesToHex helper, createLinkMapFromLinker factory; Linker.generateJsonMap() method; CLI `--map-format json|text` flag replacing `--map`; Compiler.link() stores both text and JSON maps; 35 new tests
- **C standard library - string operations** - `strlen`, `strcpy`, `strncpy`, `strcmp`, `strcat`, `strchr`, `strtoi`, `atoi` implemented as C source files in `src/core/stdlib/`; 17 new tests
- **FILE stream abstractions and stdio intrinsics** - `FILE` struct type registered in TypeRegistry with 4 fields (buffer: char*, bufSize: int, bufPos: int, flags: int) totaling 8 bytes; stdio intrinsics added to IntrinsicMap (`__fputc`, `__fgetc`, `__feof`, `__ferror`, `__fopen`, `__fclose`); Z80 codegen for FPUTC (write char to buffer+bufPos, increment bufPos), FGETC (read from buffer+bufPos with EOF check), FEOF (check bit 0 of flags), FERROR (check bit 1 of flags), FOPEN (allocate 8 bytes on stack, zero-initialize), FCLOSE (zero all fields); parser fixed for correct unary operator precedence (`!f(x)` now parses as `UnaryOp(!, Call(f, x))` instead of `Call(UnaryOp(!, f), x)`); 28 new stdio tests
- **Standard library header files** - Created `<stdint.h>`, `<stdlib.h>`, `<stdio.h>`, `<file.h>` header files in `src/core/stdlib/` with type definitions, constants, and function declarations; `stdint.h` provides `uint8_t`, `uint16_t`, `uint32_t`, `int8_t`, `int16_t`, `int32_t`, `size_t`, `ptrdiff_t`, `uintptr_t`, `intptr_t` typedefs and `NULL`, `EOF`, `SIZE_MAX`, `INT_MAX`, `INT_MIN`, `UINT_MAX`, `CHAR_BIT` constants; `stdlib.h` declares `memset`, `memcpy`, `memmove`, `memcmp`, `memchr`, `atoi`, `strtoi`; `stdio.h` declares `__fputc`, `__fgetc`, `__feof`, `__ferror`, `__fopen`, `__fclose`, `__serial_open`, `__serial_close`, `__serial_read`, `__serial_write`, `__serial_available`, `__terminal_open`, `__terminal_close`; `file.h` defines `FILE` struct (7 fields, 12 bytes) and stream type constants (`STREAM_FILESYSTEM`, `STREAM_SERIAL`, `STREAM_TERMINAL`); parser extended to support `typedef struct/union {...} name;` declarator syntax; translation context handles typedef struct nodes in second pass; pragma directive results filtered during tokenization; 55 new tests
- **Soft-float math functions** - `fabsf` (absolute value), `floorf` (round toward negative infinity), `ceilf` (round toward positive infinity), `modff` (split into integer and fractional parts), `frexpf` (decompose into mantissa and exponent), `ldexpf` (multiply by 2^exponent) compiled to `_float_abs`, `_float_floor`, `_float_ceil`, `_float_modf`, `_float_frexpf`, `_float_ldexpf` calls; Expression translator maps C function names to internal float function names via `floatFuncMapping`; `float_abs.z80` and `float_floor.z80` soft-float assembly implementations in `src/core/stdlib/`; `float.h` extended with ceil/floor declarations; 42 new tests
